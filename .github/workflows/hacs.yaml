name: HACS

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    name: HACS
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: HACS Action
        uses: hacs/action@main
        with:
          category: integration
